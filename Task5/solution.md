### Solution

Follow below steps:
- Search for  ```volumes``` select ec2 volumes option.
- Click create volume.
- Choose volume type as ```gp3```.
- Put volume size as ```2 gb```. And keep everything as default.
- Now click on add tag, put ```Name``` as tag value and ```devops-volume``` as value of tag.
- Click create volume.
